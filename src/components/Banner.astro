---
const { player, index } = Astro.props
const id = index + 1
---

<li class='flex mx-auto w-[350px] border border-sky-600 p-3 bg-slate-700 rounded-lg'>
    <aside class='w-[300px]'>
        <h2 transition:name={`hpl-${id}`} class='text-2xl font-bold'>{ player['full name'] }</h2>
        <a class='text-blue-500 hover:underline' href={`/${id}`} >View more</a>
    </aside>
    <img
        transition:name={`pl-${id}`}
        class='w-[200px] mx-auto hover:bg-[#00000017] rounded-lg'
        src={player.image}
    />
</li>